// calls for various filesystem operations here
struct FileOpt {
    // FileOpt will serve as the intreface for 
    // most operations which require direct 
    // file interactions.
    filename: str,
    pathname: str,
    imagename: str
}

impl FileOpt {
    // Standard file operations 
    // and network file operations.
    pub fn create_pxe_image(image_name: str, )
}


